<% if current_user.following?(user) %>
  <%= link_to "フォロー外す", user_follows_path(user.id), method: :delete, class: "btn btn-primary" %>
<% else %>
  <%= link_to "フォローする", user_follows_path(user.id), method: :post, class: "btn btn-success" %>
<% end %>